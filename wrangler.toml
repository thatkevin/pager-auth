name               = "pager-auth"
main               = "index.js"
compatibility_date = "2024-09-23"

[vars]
# GitHub App client ID (not a secret — safe to commit).
# Fill in after creating the GitHub App at github.com/settings/apps.
GITHUB_CLIENT_ID = "Iv23li7Ksx4uBQtXTtW1"

# GITHUB_CLIENT_SECRET must be added as a secret in the Cloudflare dashboard
# (Settings → Secrets) so it is never committed to the repo.
